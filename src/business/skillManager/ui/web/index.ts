'use client';

/**
 * skillManager Web UI — `sa2kit/business/skillManager/ui/web`
 * 鉴权：页面使用 AuthGuard；宿主须用 AuthProvider（`@profile/auth/react`）包裹。
 */

export { default as SkillManagerPage } from './pages/SkillManagerPage';
export * from './services/skillManagerClient';
