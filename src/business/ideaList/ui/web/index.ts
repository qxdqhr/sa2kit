'use client';

/**
 * ideaList Web UI — `sa2kit/business/ideaList/ui/web`
 * 鉴权：页面使用 AuthGuard；宿主须用 AuthProvider（`@profile/auth/react`）包裹。
 */

export { default as IdeaListPage } from './pages/IdeaListPage';
