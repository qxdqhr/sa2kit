/**
 * Auth 展示壳：动森视觉，无 Better Auth 逻辑。
 * 组装层（hooks + headless）仍在 `sa2kit/common/auth/components`。
 */
export {
  AuthModalShell,
  AuthField,
  AuthTextInput,
  AuthModeChips,
  AuthError,
  AuthLinkButton,
  AuthSubmitButton,
  AuthSwitchRow,
  AuthGateFallback,
  AuthLoadingGate,
} from './shell';
