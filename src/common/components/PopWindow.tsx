'use client';

/**
 * @deprecated 请改用 `sa2kit/common/ui/admin`（Modal / ConfirmModal）。
 * 本文件仅作兼容再导出，实现已迁至动森 admin 门面。
 */
export { Modal, type ModalProps } from '../ui/admin/Modal';
export { ConfirmModal, type ConfirmModalProps } from '../ui/admin/ConfirmModal';
